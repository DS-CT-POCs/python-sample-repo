# TODOs and sample issues

These items are here so the RAG pipeline can answer questions like "What TODOs exist?" or "What issues are open?".

1. **Add input validation to `math_ops.py`** — Functions like `add` and `multiply` do not validate types; consider rejecting non-numeric inputs or handling NaN/Inf.
2. **Support optional locale in `greet.py`** — Allow localized greetings (e.g. "Hola", "Bonjour") via a locale parameter or external string table.
